server:
  port: 8085

spring:
  application:
    name: inventory-service
  datasource:
    url: jdbc:h2:mem:inventoryDB
    username: sa
    password: password
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
